export const metadata = {
  title: "Case Studies | Agentic",
  description: "See how we've helped brands grow through precision digital marketing.",
};

export default function CasesLayout({ children }) {
  return <>{children}</>;
}
